# Historyczne wykresy

Pliki PNG w tym katalogu pochodzą z wcześniejszych eksperymentów.
Nie przedstawiają wyników poprawionego protokołu `group_refit_v2` i nie
powinny być cytowane jako aktualne wyniki pracy. Nowe wykresy powstają
w `results/runs/<run-id>/seed-<seed>/` po ukończonej ewaluacji.
